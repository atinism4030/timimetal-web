import { IProject } from "./types";

export const projects: IProject[] = [];

// These are now stubs. Use the actions in app/actions/projects.ts instead
export const fetchProjecst = async (numberOfProjects?: number) => {
  return [];
};

export const getProjectById = async (id: string | number) => {
  return null;
};
